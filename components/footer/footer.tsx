import React from "react";

export default function Footer() {

    const version = () => {
        
    }

    const content = (version: string) => {
        <div>
            {version}
        </div>
    }
    
    return (

    );
}